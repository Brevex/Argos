[package]
name = "argos_core"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
publish.workspace = true
license.workspace = true

[features]
# Test fixtures (in-memory block source) shared with dependent crates' test suites.
test-util = []

[dependencies]

[dev-dependencies]
argos_core = { workspace = true, features = ["test-util"] }

[lints]
workspace = true
