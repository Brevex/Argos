{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The commands the window may call, the engine event channel, the window controls it draws itself — including the two a close goes through, since a run under way is confirmed before the window goes — and a folder picker. No shell, no network, no filesystem access, and no path on disk the web view can read: everything a run recovers goes to the destination folder, and the window never reads it back.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:event:allow-listen",
    "core:event:allow-unlisten",
    "core:window:allow-start-dragging",
    "core:window:allow-minimize",
    "core:window:allow-toggle-maximize",
    "core:window:allow-close",
    "core:window:allow-destroy",
    "dialog:allow-open"
  ]
}
