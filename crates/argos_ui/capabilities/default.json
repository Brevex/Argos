{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The four commands the window may call, the engine event channel, the window controls it draws itself, and a folder picker. No shell, no filesystem, no network, and no path into a session directory: the window shows counts and progress, never recovered content.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:event:allow-listen",
    "core:event:allow-unlisten",
    "core:window:allow-start-dragging",
    "core:window:allow-minimize",
    "core:window:allow-toggle-maximize",
    "core:window:allow-close",
    "dialog:allow-open"
  ]
}
