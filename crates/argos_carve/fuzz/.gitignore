target/
artifacts/
coverage/

# libFuzzer writes its own discoveries back into the corpus directories under
# their SHA-1. Those are not seeds: they are unbounded, hash-named and carry no
# intent. A seed is authored by `examples/gen_fuzz_corpus.rs` and named for the
# case it covers (A-SUPPORT-DECLARED).
corpus/*/????????????????????????????????????????
